package com.sedooj.api.domain.data.types

import com.sedooj.ui_kit.R
enum class GenderType(val title: Int) {
    NOT_SELECTED(title = R.string.not_selected),
    MALE(title = R.string.male),
    FEMALE(title = R.string.female)
}